import React, {FC} from 'react';

type PageProps = {
  params: {
    searchTerm: string,
  }

};

const Page: FC<PageProps> = ({params}) => {
  return (
    <div>

    </div>
  );
};

export default Page;